# Private seed artifacts

`full-history.zip` and `snapshot-manifest.json` are generated locally from a copy of the production database. They contain sensitive commercial history and must not be committed to version control or uploaded to any external service.
